# 机械键盘按键模型生成器 - 实现方案

## 一、项目概述

开发一个参数化的机械键盘按键（键帽）3D模型生成器，支持自定义字体、字母、按键尺寸和斜角等参数，生成分离的按键本体和文字模型，便于多色3D打印。

## 二、技术栈选择

### 2.1 核心技术
- **GUI框架**: PyQt5
- **3D建模库**: 推荐使用 **CadQuery** 或 **pythonocc-core**
  - **CadQuery**: 基于OpenCASCADE，API简洁，支持STL和STEP导出，学习曲线平缓
  - **pythonocc-core**: OpenCASCADE的Python绑定，功能强大但API较复杂
  - **备选**: FreeCAD Python API（功能全面但体积大）
- **3D预览**: 
  - **VTK** + PyQt5（推荐，功能强大，渲染效果好）
  - **PyOpenGL**（轻量级，需要更多手动实现）
- **字体处理**: 
  - **fontTools** 或 **freetype-py**（提取字体轮廓）
  - **shapely**（2D几何处理，将字体轮廓转换为几何图形）

### 2.2 依赖库清单
```
PyQt5 >= 5.15.0
cadquery >= 2.3.0  # 或 pythonocc-core >= 7.6.0
vtk >= 9.0.0
numpy >= 1.21.0
fonttools >= 4.28.0
shapely >= 2.0.0
```

## 三、软件架构设计

### 3.1 模块划分

```
KeyGenerator/
├── main.py                 # 程序入口
├── ui/                     # UI界面模块
│   ├── __init__.py
│   ├── main_window.py      # 主窗口
│   ├── parameter_panel.py  # 参数输入面板
│   ├── preview_widget.py   # 3D预览组件
│   └── font_selector.py    # 字体选择器
├── core/                   # 核心业务逻辑
│   ├── __init__.py
│   ├── keycap_modeler.py   # 按键模型生成器
│   ├── text_modeler.py     # 文字模型生成器
│   └── parameters.py       # 参数管理类
├── geometry/               # 几何计算模块
│   ├── __init__.py
│   ├── keycap_shape.py     # 按键形状定义
│   ├── text_extrusion.py   # 文字挤出处理
│   └── font_processor.py   # 字体轮廓处理
├── export/                 # 导出模块
│   ├── __init__.py
│   ├── stl_exporter.py     # STL导出
│   └── step_exporter.py    # STEP导出
└── utils/                  # 工具函数
    ├── __init__.py
    └── file_utils.py       # 文件操作工具
```

### 3.2 核心类设计

#### 3.2.1 参数管理类 (Parameters)
```python
class KeycapParameters:
    - key_width: float          # 按键宽度 (mm)
    - key_height: float         # 按键高度 (mm)
    - key_depth: float          # 按键深度 (mm)
    - top_angle: float          # 顶部斜角 (度)
    - side_angle: float         # 侧面斜角 (度)
    - wall_thickness: float     # 壁厚 (mm)
    - stem_type: str            # 轴体类型 (MX, Alps等)
    - font_path: str            # 字体文件路径
    - letter: str               # 字母/字符
    - text_height: float        # 文字高度 (mm)
    - text_depth: float         # 文字深度 (mm)
```

#### 3.2.2 按键模型生成器 (KeycapModeler)
- 根据参数生成按键本体几何
- 支持不同轴体类型（MX、Alps等）
- 生成按键的顶部、侧面、底部结构
- 处理斜角和圆角

#### 3.2.3 文字模型生成器 (TextModeler)
- 从字体文件提取字符轮廓
- 将2D轮廓转换为3D模型（挤出）
- 处理文字在按键表面的位置和深度
- 生成独立的文字模型（用于多色打印）

#### 3.2.4 预览组件 (PreviewWidget)
- 使用VTK渲染3D模型
- 支持旋转、缩放、平移
- 显示按键和文字模型
- 支持切换显示/隐藏各部分

## 四、功能模块详细设计

### 4.1 UI界面设计

#### 主窗口布局
```
┌─────────────────────────────────────────┐
│  菜单栏: 文件(新建/打开/保存/导出)      │
├──────────────┬──────────────────────────┤
│              │                          │
│  参数面板    │    3D预览窗口            │
│              │                          │
│  - 字体选择  │    [旋转/缩放控制]       │
│  - 字母输入  │                          │
│  - 尺寸参数  │                          │
│  - 斜角参数  │                          │
│  - 文字参数  │                          │
│              │                          │
│  [生成模型]  │                          │
│  [导出STL]   │                          │
│  [导出STEP]  │                          │
│              │                          │
└──────────────┴──────────────────────────┘
```

#### 参数面板功能
1. **字体选择器**
   - 系统字体列表下拉框
   - 字体预览
   - 自定义字体文件选择

2. **字母输入**
   - 单字符输入框
   - 字符预览

3. **按键尺寸**
   - 宽度、高度、深度输入（单位：mm）
   - 壁厚设置

4. **斜角设置**
   - 顶部斜角滑块/输入框（0-45度）
   - 侧面斜角滑块/输入框（0-30度）

5. **文字参数**
   - 文字高度（mm）
   - 文字深度（mm）
   - 文字位置（X、Y偏移）

6. **轴体类型**
   - 下拉选择（MX、Alps等）

### 4.2 3D模型生成流程

#### 按键本体生成流程
```
1. 创建基础矩形（按键顶部）
2. 应用顶部斜角（倒角或倾斜）
3. 向下挤出形成侧面
4. 应用侧面斜角
5. 创建底部结构
6. 添加轴体连接结构（根据轴体类型）
7. 应用圆角（可选）
```

#### 文字模型生成流程
```
1. 加载字体文件
2. 提取字符轮廓（使用fontTools/freetype）
3. 将轮廓转换为2D几何图形（shapely）
4. 缩放和定位文字
5. 挤出为3D模型（指定深度）
6. 与按键表面求差（生成凹陷）或独立生成（凸起）
```

### 4.3 导出功能

#### STL导出
- 使用CadQuery的exportStl()方法
- 分别导出按键本体和文字模型
- 支持二进制和ASCII格式

#### STEP导出
- 使用CadQuery的exportStep()方法
- 保持参数化特征（如果可能）
- 分别导出或组合导出

## 五、关键技术难点与解决方案

### 5.1 字体轮廓提取
**难点**: 从TTF/OTF字体文件提取字符轮廓并转换为3D几何

**解决方案**:
- 使用fontTools提取字形轮廓点
- 使用shapely将点集转换为多边形
- 处理复杂字形（包含多个轮廓、孔洞等）

### 5.2 文字在按键表面的定位
**难点**: 确保文字正确放置在按键表面，考虑斜角影响

**解决方案**:
- 计算按键表面中心点
- 根据斜角调整文字位置和方向
- 使用投影或布尔运算确保贴合

### 5.3 轴体连接结构
**难点**: 不同轴体类型（MX、Alps等）的连接结构不同

**解决方案**:
- 预定义常见轴体类型的几何参数
- 使用参数化方式生成连接结构
- 支持自定义轴体参数

### 5.4 性能优化
**难点**: 复杂模型生成和预览可能较慢

**解决方案**:
- 使用多线程进行模型生成
- 预览时使用简化模型
- 添加进度提示

## 六、开发计划（建议）

### 阶段一：基础框架搭建（1-2天）
- [ ] 创建项目结构
- [ ] 搭建PyQt5主窗口
- [ ] 实现基础参数面板
- [ ] 集成VTK预览窗口

### 阶段二：按键模型生成（2-3天）
- [ ] 实现基础按键几何生成
- [ ] 添加斜角功能
- [ ] 实现轴体连接结构
- [ ] 测试不同参数组合

### 阶段三：文字模型生成（2-3天）
- [ ] 实现字体轮廓提取
- [ ] 实现文字3D挤出
- [ ] 实现文字定位和贴合
- [ ] 处理复杂字符

### 阶段四：导出与优化（1-2天）
- [ ] 实现STL导出
- [ ] 实现STEP导出
- [ ] 性能优化
- [ ] 错误处理完善

### 阶段五：测试与完善（1-2天）
- [ ] 功能测试
- [ ] UI优化
- [ ] 文档编写
- [ ] 打包发布准备

## 七、技术选型建议

### 推荐方案：CadQuery + VTK
**优点**:
- CadQuery API简洁，易于上手
- 支持STL和STEP导出
- 基于OpenCASCADE，功能强大
- VTK渲染效果好，交互流畅

**缺点**:
- CadQuery文档相对较少
- 某些高级功能可能需要直接使用OpenCASCADE

### 备选方案：pythonocc-core + VTK
**优点**:
- 直接使用OpenCASCADE，功能最全面
- 社区支持较好

**缺点**:
- API较复杂，学习曲线陡
- 代码量可能更多

## 八、注意事项

1. **字体版权**: 提醒用户注意字体使用许可
2. **模型精度**: 确保生成的模型适合3D打印（壁厚、细节等）
3. **文件格式**: 考虑支持更多格式（OBJ、PLY等）
4. **参数验证**: 确保输入参数合理（避免负值、过大值等）
5. **错误处理**: 完善的异常处理和用户提示

## 九、扩展功能（可选）

- 支持多字符按键（如"Enter"、"Shift"等）
- 支持图标/符号输入
- 支持按键表面纹理
- 支持按键组合预览（整个键盘布局）
- 支持参数预设保存和加载
- 支持批量生成

---

**下一步**: 请确认技术选型（推荐CadQuery）和开发计划，我将开始实现代码。
