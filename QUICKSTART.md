# 快速开始指南

## 安装步骤

1. **安装Python依赖**
   ```bash
   pip install -r requirements.txt
   ```

2. **运行程序**
   ```bash
   python main.py
   ```

## 使用说明

### 基本操作流程

1. **选择字体**
   - 从下拉列表选择系统字体，或点击"浏览..."选择自定义字体文件

2. **输入字母**
   - 在"字母"输入框中输入单个字符（如 "A", "B", "1" 等）

3. **设置按键尺寸**
   - **宽度/高度**: 标准按键通常为18mm x 18mm
   - **深度**: 按键从顶部到底部的深度，通常8-10mm
   - **壁厚**: 按键壁的厚度，建议1-1.5mm

4. **设置斜角**
   - **顶部斜角**: 按键顶部的倾斜角度（0-45度）
   - **侧面斜角**: 按键侧面的倾斜角度（0-30度）

5. **设置文字参数**
   - **文字高度**: 文字的大小（mm）
   - **文字深度**: 
     - 正值：凹陷文字（从表面向下）
     - 负值：凸起文字（从表面向上）

6. **选择轴体类型**
   - **MX**: 最常见的Cherry MX轴体（十字形）
   - **Alps**: Alps轴体（矩形）

7. **生成模型**
   - 点击"生成模型"按钮
   - 等待模型生成完成
   - 在右侧预览窗口中查看3D模型

8. **导出文件**
   - **导出STL**: 文件 -> 导出STL (Ctrl+S)
   - **导出STEP**: 文件 -> 导出STEP (Ctrl+P)
   - 会分别生成按键和文字两个文件（便于多色打印）

### 预览窗口操作

- **旋转**: 按住鼠标左键拖动
- **缩放**: 鼠标滚轮
- **平移**: 按住鼠标右键拖动
- **重置视角**: 双击预览窗口

### 常见参数设置

#### 标准Cherry MX按键
- 宽度: 18mm
- 高度: 18mm
- 深度: 8mm
- 壁厚: 1mm
- 轴体类型: MX

#### 大按键（如空格键）
- 宽度: 根据实际尺寸（如100mm）
- 高度: 18mm
- 其他参数同上

## 注意事项

1. **字体选择**: 确保选择的字体包含要生成的字符
2. **模型精度**: 生成的模型已针对3D打印优化，但建议检查壁厚是否足够
3. **文字深度**: 
   - 凹陷文字（正值）适合单色打印后上色
   - 凸起文字（负值）适合多色打印
4. **文件导出**: STL格式更适合3D打印，STEP格式保留更多几何信息

## 故障排除

### 模型生成失败
- 检查参数是否合理（不能为负值或过大）
- 检查字体文件是否有效
- 检查字母是否在字体中可用

### 预览窗口不显示
- 确保已安装VTK
- 检查显卡驱动是否正常

### 导出失败
- 确保有写入权限
- 检查磁盘空间
- 确保模型已成功生成

## 技术说明

- **3D建模**: 使用CadQuery库
- **字体处理**: 使用fontTools提取字体轮廓
- **3D预览**: 使用VTK进行渲染
- **导出格式**: 支持STL（3D打印）和STEP（CAD软件）
